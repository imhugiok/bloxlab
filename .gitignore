# OS / editor
.DS_Store
Thumbs.db
*.log

# Local env / secrets (never commit)
.env
.env.local

# Node (not used by the plugin, but just in case)
node_modules/

# Local Claude Code config (per-user, not part of the plugin)
.claude/
